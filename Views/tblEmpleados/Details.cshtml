@model AriasRomanJonathan_Proyecto1.tblEmpleados

@{
    ViewBag.Title = "Información CRUD";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Title</h2>

<div>
    <h4>tblEmpleados</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nombre)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Apellidos)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Apellidos)
        </dd>

    </dl>
</div>
<p>
   <a href="@Url.Action("Edit", new { id = Model.ID })" class="btn btn-warning">Editar</a>
   <a href="@Url.Action("Index")" class="btn btn-secondary">Regresar</a>
</p>
